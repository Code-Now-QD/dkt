                               每次更改框架等决定因素后，一定要在主分支同步更改；
---------------------------------------------------------------------------------------------------

script.py       -- 通用集成脚本
datavisualization.py    -- 用于数据集可视化
label_update.py	--用于修改label标签，将标签改为指定标签；
extract_picture.py	--用于抽帧；
quantity.py	-- 用于检索数据集label总量，及每个class占比，以便统计文本
Prefix_naming.py    -- 指定前缀并按顺序重命名图片文件；

---------------------------------------------------------------------------------------------------

extract_4k_60.py    -- 用于从视频文件中抽帧；
new_name.py  -- 替代name.py，可选前缀按顺序重命名文件；
keep_one.py	--用于比较两个目录中的文件，并删除那些在其中一个目录存在，但在另一个目录不存在的文件；
fixed_zoom	--数据增强其一，缩放图片
image_path.py   -- 更改图片路径 (根据json文件名)，以便json正确访问图片的位置
imageData.py	-- 更新图片信息的值；
json_txt.py	-- json转yolov格式txt

---------------------------------------------------------------------------------------------------
数据集可视化.py   -- 字面意思，外套脚本
del_json.py    -- 删除序号在指定范围内的文件，终端命令可直接完成；
pillow1.py	-- 给文件添加前缀名

---------------------------------------------------------------------------------------------------
